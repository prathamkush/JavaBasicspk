public class Operators {
    public static void main(String[] args){
        //Arithmetic -> [+,-,*,/,++,%,--]
        // Logical -> [&&, ||, !]
        // Relational -> [>, <, >=, <=,!=, ==]
        // Bitwise -> [&, |, ^, !, <<, >>, >>>(0 fill right shift)]
        int a = 10; //1010
        int b = 11; //1011
        int c = a&b; //1010 (examples)
        System.out.println(c);

        // Assignment -> [=, +=, -=, *= etc]
        // Miscellaneous -> [terenary {a==10?true:false}, typecast, instanceof, ]
    }
}
